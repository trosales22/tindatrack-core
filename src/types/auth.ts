export interface LoginPayload {
    user_id: string;
    password: string;
}

export interface RegisterBusinessAdminPayload {
    email: string;
    firstname: string;
    lastname: string;
    password: string;
}